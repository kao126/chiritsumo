<div class="container">
  <div>
    <%= image_tag @post.get_image(470, 580) %>
  </div>
  <div>
    <p id="favorites_btn_<%= @post.id %>">
      <%= render 'public/favorites/btn', post: @post %>
    </p>
    <p><%= @post.caption %></p>
  </div>
    <%= link_to 'Edit', edit_post_path(@post) %>
    <%= link_to 'Destroy', post_path(@post), method: :delete, data: { confirm: '本当に消しますか？' } %>
</div>