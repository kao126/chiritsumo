<div class="container pt-md-5 my-5">
  <div class="row">
    <div class="col-lg-8 mx-auto">
      <div class="customer-devise-form">
        <div class="my-2">
          <h7 class="ml-5">会員の方はこちらから<strong>ログイン</strong></h7>
          <div class="col-md-8 mx-auto px-0">
            <%= form_with model: @customer, url: customer_session_path, method: :post do |f| %>
              <div class="form-item my-3">
                <%= f.label "メールアドレス" %>
                <%= f.email_field :email, autofocus: true, autocomplete: "off", class:"col devise-form-style" %>
              </div>
              <div class="form-item mb-4">
                <%= f.label "パスワード" %>
                <%= f.password_field :password, autocomplete: "current-password", class:"col devise-form-style" %>
              </div>
              <div class="form-item d-flex justify-content-end">
                <%= f.submit "ログイン", class:"customer-devise-submit" %>
              </div>
            <% end %>
          </div>
        </div>
        <div class="mt-3">
          <h7 class="ml-5">会員登録がお済みでない方</h7>
          <p class="text-center my-2"><%= link_to "こちら", new_customer_registration_path %> から新規登録を行ってください。</p>
        </div>
      </div>
    </div>
  </div>
</div>