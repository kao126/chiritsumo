<%= form_with(model:[post, post_comment], local: false) do |f| %>
  <%= f.text_area :comment, rows:'2',placeholder: "コメントをここに", class: "w-100 comment-textarea" %>
  <div class="text-right">
    <%= f.submit "送信する", class: "post-submit" %>
  </div>
<% end %>